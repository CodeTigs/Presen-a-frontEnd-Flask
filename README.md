# Presen-a-frontEnd-Flask
Resumo da Integração
1 - Rota Flask (/api/calcular-produtividade): Recebe JSON, processa lógica Python, retorna JSON.

2 - HTML: Cria a interface visual usando o sistema de classes existente (page, btn-primary).

3 - JavaScript: Intercepta o submit, faz o fetch assíncrono e atualiza o DOM sem recarregar a página.
